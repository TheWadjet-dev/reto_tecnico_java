# Configuracion del servidor
server.port=${SERVER_PORT:8080}
server.servlet.context-path=/

# Conexion a base de datos usando variables de entorno
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://aws-0-us-east-1.pooler.supabase.com:6543/postgres?prepareThreshold=0&preparedStatementCacheQueries=0}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres.qvpnoivjuqzpgyxjpizr}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:Wadjet140400*}
spring.datasource.driver-class-name=${SPRING_DATASOURCE_DRIVER_CLASS_NAME:org.postgresql.Driver}

# Configuracion de JPA/Hibernate usando variables de entorno
spring.jpa.database-platform=${SPRING_JPA_DATABASE_PLATFORM:org.hibernate.dialect.PostgreSQLDialect}
spring.jpa.hibernate.ddl-auto=${SPRING_JPA_HIBERNATE_DDL_AUTO:create-drop}
spring.jpa.show-sql=${SPRING_JPA_SHOW_SQL:true}
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.jdbc.batch_size=20
spring.jpa.properties.hibernate.order_inserts=true
spring.jpa.properties.hibernate.order_updates=true
spring.jpa.properties.hibernate.jdbc.batch_versioned_data=true

# Configuracion de logging
logging.level.com.example.ejercicio=DEBUG
logging.level.org.springframework.web=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# Configuracion de Spring Boot Actuator
management.endpoints.web.exposure.include=health,info,metrics
management.endpoint.health.show-details=always

# Configuracion de validacion
spring.web.resources.add-mappings=false

# Configuracion de la aplicacion
app.name=Ejercicio Tecnico Backend Java
app.version=1.0.0
app.description=API REST para gesti�n de usuarios